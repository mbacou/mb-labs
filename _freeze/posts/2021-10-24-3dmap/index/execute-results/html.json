{
  "hash": "41caaad82beb36d5c718fd163d743331",
  "result": {
    "engine": "knitr",
    "markdown": "---\ntitle: \"Visualizing Water: Interactive 3D Map of River Basins (draft)\"\ndescription: |\n  Part 3: how to represent water flux over 3D maps of river basins using WebGL.\ndate: 2021-10-24\ndraft: true\nfreeze: true\npreview: ./fig/preview.png\nbibliography: biblio.bib\nnocite: \"@*\"\ncategories:\n  - draft\n  - imwi\n  - hydrology\n---\n\n\n::: {.cell}\n\n```{.r .cell-code}\nlibrary(knitr)\nlibrary(scales)\nlibrary(stringr)\nlibrary(data.table)\nlibrary(raster)\nlibrary(osmdata)\n```\n\n::: {.cell-output .cell-output-error}\n\n```\nError in `library()`:\n! there is no package called 'osmdata'\n```\n\n\n:::\n\n```{.r .cell-code}\nlibrary(rayshader)\n```\n\n::: {.cell-output .cell-output-error}\n\n```\nError in `library()`:\n! there is no package called 'rayshader'\n```\n\n\n:::\n\n```{.r .cell-code}\nlibrary(rgl)\n```\n\n::: {.cell-output .cell-output-error}\n\n```\nError in `library()`:\n! there is no package called 'rgl'\n```\n\n\n:::\n\n```{.r .cell-code}\nsetupKnitr(autoprint = TRUE)\n```\n\n::: {.cell-output .cell-output-error}\n\n```\nError in `setupKnitr()`:\n! could not find function \"setupKnitr\"\n```\n\n\n:::\n\n```{.r .cell-code}\nif (interactive()) {\n  setwd(\"posts/2021-10-24-3dmap/\")\n}\n\nroot <- \"/media/mbacou/Backup MB/WADashboard\"\n\n# Default color palette\npal <- readRDS(\"../../_assets/pal_iwmi.rds\")\n```\n\n::: {.cell-output .cell-output-error}\n\n```\nError in `gzfile()`:\n! cannot open the connection\n```\n\n\n:::\n\n```{.r .cell-code}\npar(font.main = 1, cex.axis = .8)\n```\n:::\n\n\n\nThis notebook is **Part 3** of an exploration to visualize results of hydrologic models. In [Part 1](../2021-10-16-svg/) we built custom HTML widgets using `D3.js`, and in [Part 2](../2021-10-12-highcharts/) we looked at rendering water fluxes using Sankey diagrams. Here we test multiple libraries to generate hillshade (3D) views of river basins and water infrastructure, in particular we want to compare [Three.js](https://threejs.org/) and [WebGL](https://developer.mozilla.org/en-US/docs/Web/API/WebGL_API) implementations.\n\nAside from rendering topography and water streams in 3D (and potentially other covariate layers), our objective is to overlay custom labels to illustrate the water cycle.\n\nAnother objective is to provide basin and sub-basin statistics, starting with precipitation, ET, soil moisture, and adding other covariates, such as population, land use allocation, and crop allocation.\n\nSome inspiration below:\n\n- Tyler Morgan [Tutorial: Adding OSM Data to Rayshader Maps](https://www.tylermw.com/adding-open-street-map-data-to-rayshader-maps-in-r/)\n- [Flood depth on 3D terrain](https://observablehq.com/@sw1227/flood-depth-map-on-3d-terrain?collection=@sw1227/geo) \n- Procedural-gl [examples](https://www.procedural.eu/)\n- [Cesium](https://sandcastle.cesium.com/?src=Globe%20Materials.html) platform for 3D Geospatial\n- [NASA Web WorldWind](https://worldwind.arc.nasa.gov/web/examples/#anchor) \n- [WebGL with Google Maps API](https://cloud.google.com/blog/products/maps-platform/using-new-webgl-powered-maps-features) on how to construct and render 3D scenes with WebGL.\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\nlist.files(\"./fig\", full.names = TRUE)[2] %>%\n  knitr::include_graphics()\n```\n\n::: {.cell-output-display}\n![](./fig/mara.png){width=48%}\n:::\n:::\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\nlist.files(\"./fig\", full.names = TRUE)[7] %>%\n  knitr::include_graphics()\n```\n\n::: {.cell-output-display}\n![](./fig/Screenshot from 2021-10-31 02-24-05.png){width=48%}\n:::\n:::\n\n\n\n# WebGL: Sample Scene Processing with Rayshader[^rs]\n\n[^rs]: https://www.rayshader.com/\n\n\n## Data Acquisition\n\nWe'll experiment with a smaller sample scene of the **Selingue Dam** in the Niger River basin.\n\n\n\n::: {.cell layout=\"l-body\" code_folding='false'}\n\n```{.r .cell-code}\nbasin <- shapefile(file.path(root, \"./mli/srtm/mli_basin.shp\"))\n```\n\n::: {.cell-output .cell-output-error}\n\n```\nError in `.local()`:\n! file.exists(x) is not TRUE\n```\n\n\n:::\n\n```{.r .cell-code}\nzoi <- shapefile(file.path(root, \"./mli/srtm/zoi.shp\"))\n```\n\n::: {.cell-output .cell-output-error}\n\n```\nError in `.local()`:\n! file.exists(x) is not TRUE\n```\n\n\n:::\n\n```{.r .cell-code}\next <- extent(zoi)\n```\n\n::: {.cell-output .cell-output-error}\n\n```\nError in `h()`:\n! error in evaluating the argument 'x' in selecting a method for function 'extent': object 'zoi' not found\n```\n\n\n:::\n\n```{.r .cell-code}\ncenter <- coordinates(zoi)\n```\n\n::: {.cell-output .cell-output-error}\n\n```\nError in `h()`:\n! error in evaluating the argument 'obj' in selecting a method for function 'coordinates': object 'zoi' not found\n```\n\n\n:::\n\n```{.r .cell-code}\n# Get CGIAR SRTM DEM at 90m\nsrtm <- getData(\"SRTM\", lon = center[, 1], lat = center[, 2], path = \"_data\") %>%\n  crop(zoi) %>%\n  mask(zoi)\n```\n\n::: {.cell-output .cell-output-error}\n\n```\nError in `h()`:\n! error in evaluating the argument 'x' in selecting a method for function 'mask': error in evaluating the argument 'x' in selecting a method for function 'crop': getData has been removed. Please use the geodata package instead.\n```\n\n\n:::\n\n```{.r .cell-code}\nsrtm\n```\n\n::: {.cell-output .cell-output-error}\n\n```\nError:\n! object 'srtm' not found\n```\n\n\n:::\n\n```{.r .cell-code}\n# Satellite basemaps\nbmap <- maptiles::get_tiles(terra::ext(zoi), \"Esri.WorldImagery\", zoom = 10) %>%\n  stack() %>%\n  crop(zoi) %>%\n  mask(zoi)\n```\n\n::: {.cell-output .cell-output-error}\n\n```\nError in `h()`:\n! error in evaluating the argument 'x' in selecting a method for function 'mask': error in evaluating the argument 'x' in selecting a method for function 'crop': error in evaluating the argument 'x' in selecting a method for function 'stack': error in evaluating the argument 'x' in selecting a method for function 'ext': object 'zoi' not found\n```\n\n\n:::\n:::\n\n\nThe basin covers a large area, so we would need 8 SRTM tiles, but 1 is enough for a proof of concept. Next we'll get a satellite basemap.\n\n\n::: {.cell code_folding='false'}\n\n```{.r .cell-code}\nplot(\n  terra::vect(basin),\n  col = pal[2],\n  border = pal[1],\n  lwd = 2,\n  main = \"Niger River Basin (Mali)\"\n)\n```\n\n::: {.cell-output .cell-output-error}\n\n```\nError in `h()`:\n! error in evaluating the argument 'x' in selecting a method for function 'plot': error in evaluating the argument 'x' in selecting a method for function 'vect': object 'basin' not found\n```\n\n\n:::\n\n```{.r .cell-code}\nplot(zoi, lty = 3, col = alpha(pal[\"red\"], .6), border = pal[\"red\"], lwd = 2, add = T)\n```\n\n::: {.cell-output .cell-output-error}\n\n```\nError in `h()`:\n! error in evaluating the argument 'x' in selecting a method for function 'plot': object 'zoi' not found\n```\n\n\n:::\n\n```{.r .cell-code}\ntext(-8, 10.5, \"Selingue Dam\\n(Mali)\", col = pal[\"red\"], cex = .7, font = 2)\n```\n\n::: {.cell-output .cell-output-error}\n\n```\nError:\n! object 'pal' not found\n```\n\n\n:::\n\n```{.r .cell-code}\ngrid()\n```\n\n::: {.cell-output .cell-output-error}\n\n```\nError in `int_abline()`:\n! plot.new has not been called yet\n```\n\n\n:::\n\n```{.r .cell-code}\nplot(terra::ext(zoi), main = \"Selingue Dam (Mali) - ESRI World Imagery\")\n```\n\n::: {.cell-output .cell-output-error}\n\n```\nError in `h()`:\n! error in evaluating the argument 'x' in selecting a method for function 'plot': error in evaluating the argument 'x' in selecting a method for function 'ext': object 'zoi' not found\n```\n\n\n:::\n\n```{.r .cell-code}\nplotRGB(bmap, add = T)\n```\n\n::: {.cell-output .cell-output-error}\n\n```\nError in `h()`:\n! error in evaluating the argument 'x' in selecting a method for function 'plotRGB': object 'bmap' not found\n```\n\n\n:::\n\n```{.r .cell-code}\ngrid(col = \"white\")\n```\n\n::: {.cell-output .cell-output-error}\n\n```\nError in `int_abline()`:\n! plot.new has not been called yet\n```\n\n\n:::\n\n```{.r .cell-code}\nplot(terra::rast(srtm), main = \"Selingue Dam (Mali) - SRTM 90m\")\n```\n\n::: {.cell-output .cell-output-error}\n\n```\nError in `h()`:\n! error in evaluating the argument 'x' in selecting a method for function 'plot': error in evaluating the argument 'x' in selecting a method for function 'rast': object 'srtm' not found\n```\n\n\n:::\n\n```{.r .cell-code}\ngrid(col = \"white\")\n```\n\n::: {.cell-output .cell-output-error}\n\n```\nError in `int_abline()`:\n! plot.new has not been called yet\n```\n\n\n:::\n:::\n\n\n\n## Scene Rendering\n\nNext we convert the 2 rasters to a matrix format that's compatible with [Rayshader](https://github.com/tylermorganwall/rayshader/) hillshading and raytracing algorithms.\n\n\n::: {.cell}\n\n```{.r .cell-code}\n# Convert rasters to rayshader matrix format\nsrtm_array <- raster_to_matrix(srtm)\n```\n\n::: {.cell-output .cell-output-error}\n\n```\nError in `raster_to_matrix()`:\n! could not find function \"raster_to_matrix\"\n```\n\n\n:::\n\n```{.r .cell-code}\n# Convert sat basemap to matrix (test)\nr <- raster_to_matrix(bmap$red)\n```\n\n::: {.cell-output .cell-output-error}\n\n```\nError in `raster_to_matrix()`:\n! could not find function \"raster_to_matrix\"\n```\n\n\n:::\n\n```{.r .cell-code}\ng <- raster_to_matrix(bmap$green)\n```\n\n::: {.cell-output .cell-output-error}\n\n```\nError in `raster_to_matrix()`:\n! could not find function \"raster_to_matrix\"\n```\n\n\n:::\n\n```{.r .cell-code}\nb <- raster_to_matrix(bmap$blue)\n```\n\n::: {.cell-output .cell-output-error}\n\n```\nError in `raster_to_matrix()`:\n! could not find function \"raster_to_matrix\"\n```\n\n\n:::\n\n```{.r .cell-code}\nbmap_array <- array(0, dim = c(nrow(r), ncol(r), 3))\n```\n\n::: {.cell-output .cell-output-error}\n\n```\nError in `h()`:\n! error in evaluating the argument 'x' in selecting a method for function 'nrow': object 'r' not found\n```\n\n\n:::\n\n```{.r .cell-code}\nbmap_array[,, 1] <- r / 255\n```\n\n::: {.cell-output .cell-output-error}\n\n```\nError:\n! object 'r' not found\n```\n\n\n:::\n\n```{.r .cell-code}\nbmap_array[,, 2] <- g / 255\n```\n\n::: {.cell-output .cell-output-error}\n\n```\nError:\n! object 'g' not found\n```\n\n\n:::\n\n```{.r .cell-code}\nbmap_array[,, 3] <- b / 255\n```\n\n::: {.cell-output .cell-output-error}\n\n```\nError:\n! object 'b' not found\n```\n\n\n:::\n\n```{.r .cell-code}\nbmap_array %>%\n  aperm(c(2, 1, 3)) %>%\n  # Stretch contrast\n  rescale(to = c(0, 1)) -> bmap_array\n```\n\n::: {.cell-output .cell-output-error}\n\n```\nError:\n! object 'bmap_array' not found\n```\n\n\n:::\n:::\n\n\n2D view of the generated basemap with satellite image overlay.\n\n\n::: {.cell code_folding='false'}\n\n```{.r .cell-code}\nsrtm_water <- srtm_array\n```\n\n::: {.cell-output .cell-output-error}\n\n```\nError:\n! object 'srtm_array' not found\n```\n\n\n:::\n\n```{.r .cell-code}\nsrtm_water[srtm_water < 353] <- 0\n```\n\n::: {.cell-output .cell-output-error}\n\n```\nError:\n! object 'srtm_water' not found\n```\n\n\n:::\n\n```{.r .cell-code}\nbase_map_sat <- srtm_array %>%\n  height_shade() %>%\n  add_overlay(bmap_array) %>%\n  add_shadow(ray_shade(srtm_array, zscale = 90)) %>%\n  add_water(detect_water(srtm_water), color = alpha(pal[\"blue\"], 0.4))\n```\n\n::: {.cell-output .cell-output-error}\n\n```\nError in `add_water()`:\n! could not find function \"add_water\"\n```\n\n\n:::\n\n```{.r .cell-code}\nplot_map(base_map_sat)\n```\n\n::: {.cell-output .cell-output-error}\n\n```\nError in `plot_map()`:\n! could not find function \"plot_map\"\n```\n\n\n:::\n:::\n\n\nThat doesn't look very clear, so instead we'll create a basemap, not using the satellite image but a built-in texture.\n\n\n::: {.cell code_folding='false'}\n\n```{.r .cell-code}\nbase_map <- srtm_array %>%\n  height_shade() %>%\n  add_overlay(\n    sphere_shade(srtm_array, texture = \"desert\", zscale = 4, colorintensity = 5),\n    alphalayer = 0.5\n  ) %>%\n  add_shadow(lamb_shade(srtm_array, zscale = 6), 0) %>%\n  add_shadow(ambient_shade(srtm_array), 0) %>%\n  add_shadow(\n    texture_shade(srtm_array, detail = 8 / 10, contrast = 9, brightness = 11),\n    0.1\n  ) %>%\n  add_water(detect_water(srtm_water), color = alpha(pal[\"blue\"], 0.4))\n\nsaveRDS(base_map, \"./_data/base_map.rds\")\n```\n:::\n\n\n\n::: {.cell rgl.margin='0'}\n\n```{.r .cell-code}\nbase_map <- readRDS(\"./_data/base_map.rds\")\n```\n\n::: {.cell-output .cell-output-error}\n\n```\nError in `gzfile()`:\n! cannot open the connection\n```\n\n\n:::\n\n```{.r .cell-code}\nplot_map(base_map)\n```\n\n::: {.cell-output .cell-output-error}\n\n```\nError in `plot_map()`:\n! could not find function \"plot_map\"\n```\n\n\n:::\n:::\n\n\nLooks better, so let's acquire and overlay spatial features from OSM. Note that water-related features for Mali are scarcely available from OSM, but most waterways are recorded.\n\n\n::: {.cell code_folding='false'}\n\n```{.r .cell-code}\nosm_roads <- opq(bbox(zoi)) %>%\n  add_osm_feature(\"highway\") %>%\n  osmdata_sf()\n\nosm_water <- opq(bbox(zoi)) %>%\n  add_osm_feature(\"water\") %>%\n  osmdata_sf()\n\nosm_waterway <- opq(bbox(zoi)) %>%\n  add_osm_feature(\"waterway\") %>%\n  osmdata_sf()\n\nosm_place <- opq(bbox(zoi)) %>%\n  add_osm_feature(\"place\") %>%\n  osmdata_sf()\n\nroad_layer <- generate_line_overlay(\n  dplyr::filter(osm_roads$osm_lines, highway %in% c(\"primary\", \"secondary\")),\n  extent = ext,\n  srtm_array,\n  linewidth = 5,\n  color = pal[\"black\"]\n)\n\nwater_layer <- generate_line_overlay(\n  osm_waterway$osm_lines,\n  extent = ext,\n  srtm_array,\n  linewidth = 3,\n  color = pal[\"blue\"]\n)\n\nplace_layer <- generate_label_overlay(\n  dplyr::filter(osm_place$osm_points, !is.na(name) & nchar(name) < 10),\n  extent = ext,\n  heightmap = srtm_array,\n  font = 2,\n  text_size = 1.6,\n  point_size = 1.6,\n  color = pal[\"black\"],\n  halo_color = \"white\",\n  halo_expand = 2,\n  halo_blur = 1,\n  halo_alpha = .9,\n  seed = 1,\n  data_label_column = \"name\"\n)\n\nscene <- base_map %>%\n  #scene <- base_map_sat %>%\n  add_overlay(road_layer) %>%\n  add_overlay(water_layer, alphalayer = 1) %>%\n  add_overlay(place_layer)\n\nsaveRDS(scene, \"./_data/scene.rds\")\n```\n:::\n\n\nFinally we'll use WebGL to render this scene in 3D. We also test adding polygon (bar) annotations.\n\n\n::: {.cell code_folding='false'}\n\n```{.r .cell-code}\namb_layer <- ambient_shade(srtm_array, zscale = 1 / 5)\n\nscene2 <- srtm_array %>%\n  height_shade() %>%\n  add_shadow(\n    texture_shade(srtm_array, detail = 8 / 10, contrast = 9, brightness = 11),\n    0\n  ) %>%\n  add_shadow(amb_layer, 0) %>%\n  add_overlay(road_layer) %>%\n  add_overlay(water_layer, alphalayer = 1) %>%\n  add_overlay(place_layer)\n\nsaveRDS(scene2, \"./_data/scene2.rds\")\n```\n:::\n\n\n\n::: {.cell rgl.margin='0'}\n\n```{.r .cell-code}\nscene <- readRDS(\"./_data/scene.rds\")\n```\n\n::: {.cell-output .cell-output-error}\n\n```\nError in `gzfile()`:\n! cannot open the connection\n```\n\n\n:::\n\n```{.r .cell-code}\nscene2 <- readRDS(\"./_data/scene2.rds\")\n```\n\n::: {.cell-output .cell-output-error}\n\n```\nError in `gzfile()`:\n! cannot open the connection\n```\n\n\n:::\n\n```{.r .cell-code}\nscene %>%\n  plot_3d(\n    resize_matrix(srtm_array, 1 / 10),\n    zscale = 20,\n    theta = 30,\n    phi = 20,\n    fov = 0,\n    zoom = 0.5,\n    family = \"Roboto Condensed\",\n    shadow = TRUE,\n    shadowcolor = pal[\"black\"],\n    solidcolor = pal[\"black\"]\n  )\n```\n\n::: {.cell-output .cell-output-error}\n\n```\nError in `plot_3d()`:\n! could not find function \"plot_3d\"\n```\n\n\n:::\n\n```{.r .cell-code}\n# Add polygon annotations\nxyz <- sf::read_sf(file.path(root, \"./mli/srtm/xyz.shp\"))\n```\n\n::: {.cell-output .cell-output-error}\n\n```\nError:\n! Cannot open \"/media/mbacou/Backup MB/WADashboard/./mli/srtm/xyz.shp\"; The file doesn't seem to exist.\n```\n\n\n:::\n\n```{.r .cell-code}\nrender_polygons(\n  xyz,\n  ext,\n  data_column_top = \"z\",\n  scale_data = 1,\n  color = alpha(pal[\"orange\"], 0.8),\n  lit = F,\n  light_intensity = 0.01,\n  clear_previous = T\n)\n```\n\n::: {.cell-output .cell-output-error}\n\n```\nError in `render_polygons()`:\n! could not find function \"render_polygons\"\n```\n\n\n:::\n\n```{.r .cell-code}\nrglwidget()\n```\n\n::: {.cell-output .cell-output-error}\n\n```\nError in `rglwidget()`:\n! could not find function \"rglwidget\"\n```\n\n\n:::\n:::\n\n\nThe generated scene (PNG image and associated JS code) is 3.5MB.\n\n\n# WebGL: Full Scene with Rayshader\n\nFollowing the approach above, we experiment with a 3D scene for the entire Niger River basin area used in the WA+ analysis.\n\n## Data Acquisition\n\n\n::: {.cell code_folding='false'}\n\n```{.r .cell-code}\n# Basin boundaries from IWMI\nzoi <- shapefile(file.path(root, \"./mli/srtm/mli_basin.shp\"))\next <- extent(zoi)\ncenter <- coordinates(zoi)\n\n# GAUL admin-2 boundaries\nadm <- sf::st_read(file.path(root, \"./mli/srtm/mli_adm2_lines.shp\"))\nplaces <- sf::st_read(file.path(root, \"./mli/srtm/mli_adm2_centroids.shp\"))\n\n# CGIAR SRTM DEM (upsampled)\nalt <- lapply(c(\"MLI\", \"GIN\", \"CIV\"), function(x) {\n  getData(\"alt\", country = x, mask = FALSE) %>%\n    crop(zoi) %>%\n    mask(zoi)\n})\nalt <- mosaic(alt[[1]], alt[[2]], alt[[3]], fun = mean)\nalt <- mask(crop(alt, zoi), zoi)\nwriteRaster(alt, file.path(root, \"./mli/srtm/mli_alt.tif\"), overwrite = T)\nalt\n\n# ESA WorldCover (clipped in QGIS)\nluc <- raster(\"~/Projects/WADashboard/shared/mli/srtm/ESA WorldCover clip.tiff\")\n\n# Note that I can't find a colormap for this raster, so switching to ESA CCI instead\nluc <- raster(file.path(root, \"./mli/srtm/mli_esa_cci_300m.tif\"))\n\n# And its colormap\npal.luc <- fread(\"~/Maps/ESA/ESA CCI Colormap.txt\")\nsetnames(pal.luc, c(\"value\", \"R\", \"G\", \"B\", \"A\", \"label\"))\npal.luc[, hex := rgb(R, G, B, maxColorValue = 255)]\n\n# We'll use GloRIC v1.0 stream network instead of OSM\n# Features are filtered to 'Class_hydr > 12'\ngloric <- sf::st_read(file.path(root, \"./mli/srtm/mli_gloric_filtered.shp\"))\n\n# Country labels\nosm_place <- opq(bbox(zoi)) %>%\n  add_osm_feature(\"place\", \"country\") %>%\n  osmdata_sf()\n\nsave(\n  zoi,\n  ext,\n  adm,\n  places,\n  alt,\n  luc,\n  pal.luc,\n  gloric,\n  osm_place,\n  file = \"./_data/osm.RData\"\n)\n```\n:::\n\n\n\n::: {.cell code_folding='false'}\n\n```{.r .cell-code}\nload(\"./_data/osm.RData\")\n```\n\n::: {.cell-output .cell-output-error}\n\n```\nError in `readChar()`:\n! cannot open the connection\n```\n\n\n:::\n\n```{.r .cell-code}\nplot(terra::rast(alt), main = \"Niger River Basin (Mali) - SRTM 1km\")\n```\n\n::: {.cell-output .cell-output-error}\n\n```\nError in `h()`:\n! error in evaluating the argument 'x' in selecting a method for function 'plot': error in evaluating the argument 'x' in selecting a method for function 'rast': object 'alt' not found\n```\n\n\n:::\n\n```{.r .cell-code}\nplot(adm, lty = 3, col = pal[\"black\"], lwd = 1, add = T)\n```\n\n::: {.cell-output .cell-output-error}\n\n```\nError in `h()`:\n! error in evaluating the argument 'x' in selecting a method for function 'plot': object 'adm' not found\n```\n\n\n:::\n\n```{.r .cell-code}\nplot(gloric, col = pal[\"blue\"], lwd = .4, add = T)\n```\n\n::: {.cell-output .cell-output-error}\n\n```\nError in `h()`:\n! error in evaluating the argument 'x' in selecting a method for function 'plot': object 'gloric' not found\n```\n\n\n:::\n\n```{.r .cell-code}\ngrid()\n```\n\n::: {.cell-output .cell-output-error}\n\n```\nError in `int_abline()`:\n! plot.new has not been called yet\n```\n\n\n:::\n\n```{.r .cell-code}\nplot(\n  terra::rast(luc),\n  col = pal.luc$hex,\n  breaks = pal.luc$value,\n  legend = F,\n  main = \"Niger River Basin (Mali) - ESA CCI 300m\"\n)\n```\n\n::: {.cell-output .cell-output-error}\n\n```\nError in `h()`:\n! error in evaluating the argument 'x' in selecting a method for function 'plot': error in evaluating the argument 'x' in selecting a method for function 'rast': object 'luc' not found\n```\n\n\n:::\n\n```{.r .cell-code}\nplot(adm, lty = 3, col = pal[\"black\"], lwd = 1, add = T)\n```\n\n::: {.cell-output .cell-output-error}\n\n```\nError in `h()`:\n! error in evaluating the argument 'x' in selecting a method for function 'plot': object 'adm' not found\n```\n\n\n:::\n\n```{.r .cell-code}\ngrid()\n```\n\n::: {.cell-output .cell-output-error}\n\n```\nError in `int_abline()`:\n! plot.new has not been called yet\n```\n\n\n:::\n:::\n\n\n## Scene Rendering\n\nSame configuration as above.\n\n\n::: {.cell}\n\n```{.r .cell-code}\n# Convert rasters to rayshader matrix format\nsrtm_array <- raster_to_matrix(alt)\n```\n\n::: {.cell-output .cell-output-error}\n\n```\nError in `raster_to_matrix()`:\n! could not find function \"raster_to_matrix\"\n```\n\n\n:::\n\n```{.r .cell-code}\nbmap <- RGB(luc, col = pal.luc$hex, breaks = c(0, pal.luc$value))\n```\n\n::: {.cell-output .cell-output-error}\n\n```\nError in `h()`:\n! error in evaluating the argument 'x' in selecting a method for function 'RGB': object 'luc' not found\n```\n\n\n:::\n\n```{.r .cell-code}\n# Convert land cover raster to matrix (test)\nr <- raster_to_matrix(bmap$red)\n```\n\n::: {.cell-output .cell-output-error}\n\n```\nError in `raster_to_matrix()`:\n! could not find function \"raster_to_matrix\"\n```\n\n\n:::\n\n```{.r .cell-code}\ng <- raster_to_matrix(bmap$green)\n```\n\n::: {.cell-output .cell-output-error}\n\n```\nError in `raster_to_matrix()`:\n! could not find function \"raster_to_matrix\"\n```\n\n\n:::\n\n```{.r .cell-code}\nb <- raster_to_matrix(bmap$blue)\n```\n\n::: {.cell-output .cell-output-error}\n\n```\nError in `raster_to_matrix()`:\n! could not find function \"raster_to_matrix\"\n```\n\n\n:::\n\n```{.r .cell-code}\nbmap_array <- array(0, dim = c(nrow(r), ncol(r), 3))\n```\n\n::: {.cell-output .cell-output-error}\n\n```\nError in `h()`:\n! error in evaluating the argument 'x' in selecting a method for function 'nrow': object 'r' not found\n```\n\n\n:::\n\n```{.r .cell-code}\nbmap_array[,, 1] <- r / 255\n```\n\n::: {.cell-output .cell-output-error}\n\n```\nError:\n! object 'r' not found\n```\n\n\n:::\n\n```{.r .cell-code}\nbmap_array[,, 2] <- g / 255\n```\n\n::: {.cell-output .cell-output-error}\n\n```\nError:\n! object 'g' not found\n```\n\n\n:::\n\n```{.r .cell-code}\nbmap_array[,, 3] <- b / 255\n```\n\n::: {.cell-output .cell-output-error}\n\n```\nError:\n! object 'b' not found\n```\n\n\n:::\n\n```{.r .cell-code}\nbmap_array <- bmap_array %>%\n  aperm(c(2, 1, 3)) %>%\n  # Stretch contrast\n  rescale(to = c(0, 1))\n```\n\n::: {.cell-output .cell-output-error}\n\n```\nError:\n! object 'bmap_array' not found\n```\n\n\n:::\n:::\n\n\n2D view of the generated basemap with land cover overlay.\n\n\n::: {.cell code_folding='false'}\n\n```{.r .cell-code}\nbase_map_luc <- srtm_array %>%\n  height_shade() %>%\n  add_overlay(bmap_array) %>%\n  add_shadow(lamb_shade(srtm_array, zscale = 6), 0.5) %>%\n  add_shadow(ambient_shade(srtm_array), 0.5)\n\nsaveRDS(base_map_luc, \"./_data/base_map_luc_basin.rds\")\n```\n:::\n\n\n\n::: {.cell rgl.margin='0'}\n\n```{.r .cell-code}\nbase_map_luc <- readRDS(\"./_data/base_map_luc_basin.rds\")\n```\n\n::: {.cell-output .cell-output-error}\n\n```\nError in `gzfile()`:\n! cannot open the connection\n```\n\n\n:::\n\n```{.r .cell-code}\nplot_map(base_map_luc)\n```\n\n::: {.cell-output .cell-output-error}\n\n```\nError in `plot_map()`:\n! could not find function \"plot_map\"\n```\n\n\n:::\n:::\n\n\nThe choice of hillshading parameters below might need tweaking.\n\n\n::: {.cell code_folding='false'}\n\n```{.r .cell-code}\nbase_map <- srtm_array %>%\n  sphere_shade(texture = \"desert\") %>%\n  #add_shadow(ray_shade(srtm_array, zscale=50))\n  add_shadow(lamb_shade(srtm_array, zscale = 6), 0.5) %>%\n  # This step takes up to 5min on 4 cores / 8GB\n  add_shadow(ambient_shade(srtm_array), 0.5)\n\nsaveRDS(base_map, \"./_data/base_map_basin.rds\")\n```\n:::\n\n\n2D view of the generated basemap with simple texture overlay.\n\n\n::: {.cell rgl.margin='0'}\n\n```{.r .cell-code}\nbase_map <- readRDS(\"./_data/base_map_basin.rds\")\n```\n\n::: {.cell-output .cell-output-error}\n\n```\nError in `gzfile()`:\n! cannot open the connection\n```\n\n\n:::\n\n```{.r .cell-code}\nplot_map(base_map)\n```\n\n::: {.cell-output .cell-output-error}\n\n```\nError in `plot_map()`:\n! could not find function \"plot_map\"\n```\n\n\n:::\n:::\n\n\nAs above, we choose to overlay place names, waterways, and level-2 administrative boundaries.\n\n\n::: {.cell code_folding='false'}\n\n```{.r .cell-code}\nadmin_layer <- generate_line_overlay(\n  adm,\n  extent = ext,\n  srtm_array,\n  linewidth = 4,\n  lty = 1,\n  color = \"white\"\n)\n\nadmin_layer2 <- generate_line_overlay(\n  adm,\n  extent = ext,\n  srtm_array,\n  linewidth = 2,\n  lty = 1,\n  color = pal[\"black\"],\n  offset = c(0, 0.03)\n)\n\nwater_layer <- generate_line_overlay(\n  gloric,\n  extent = ext,\n  srtm_array,\n  linewidth = 6,\n  color = pal[\"light-blue\"],\n  data_column_width = \"Class_hydr\"\n)\n\nplace_layer <- generate_label_overlay(\n  places,\n  extent = ext,\n  heightmap = srtm_array,\n  font = 2,\n  text_size = 1.6,\n  point_size = 0,\n  color = \"white\",\n  halo_color = pal[\"black\"],\n  halo_expand = 2,\n  halo_blur = 2,\n  halo_alpha = .6,\n  seed = 1,\n  data_label_column = \"ADM2_NAME\"\n)\n\ncountry_layer <- generate_label_overlay(\n  osm_place$osm_points,\n  extent = ext,\n  heightmap = srtm_array,\n  offset = c(0.1, -0.5),\n  font = 3,\n  text_size = 3,\n  point_size = 0,\n  color = pal[\"light\"],\n  halo_color = pal[\"black\"],\n  halo_expand = 4,\n  halo_blur = 4,\n  halo_alpha = .6,\n  halo_offset = c(0.1, -0.5),\n  seed = 1,\n  data_label_column = \"name\"\n)\n\nscene <- base_map %>%\n  add_overlay(admin_layer) %>%\n  add_overlay(water_layer) %>%\n  add_overlay(place_layer)\n\nplot_map(scene)\nsaveRDS(scene, \"./_data/scene_basin.rds\")\n\nscene_luc <- base_map_luc %>%\n  add_overlay(admin_layer2, alphalayer = .8) %>%\n  add_overlay(admin_layer, alphalayer = .9) %>%\n  add_overlay(country_layer) %>%\n  add_overlay(water_layer) %>%\n  add_overlay(place_layer)\n\nplot_map(scene_luc)\nsaveRDS(scene_luc, \"./_data/scene_luc_basin.rds\")\n```\n:::\n\n\nFinally we'll use WebGL to render these scenes in 3D.\n\n\n::: {.cell rgl.margin='0'}\n\n```{.r .cell-code}\nscene <- readRDS(\"./_data/scene_basin.rds\")\n```\n\n::: {.cell-output .cell-output-error}\n\n```\nError in `gzfile()`:\n! cannot open the connection\n```\n\n\n:::\n\n```{.r .cell-code}\nscene %>%\n  plot_3d(\n    resize_matrix(srtm_array, 1 / 5),\n    zscale = 40,\n    linewidth = 0,\n    theta = 15,\n    phi = 45,\n    fov = 0,\n    zoom = 0.4,\n    family = \"Roboto Condensed\",\n    shadow = TRUE,\n    shadowcolor = pal[\"black\"],\n    solidcolor = pal[\"black\"]\n  )\n```\n\n::: {.cell-output .cell-output-error}\n\n```\nError in `plot_3d()`:\n! could not find function \"plot_3d\"\n```\n\n\n:::\n\n```{.r .cell-code}\nrglwidget()\n```\n\n::: {.cell-output .cell-output-error}\n\n```\nError in `rglwidget()`:\n! could not find function \"rglwidget\"\n```\n\n\n:::\n\n```{.r .cell-code}\nrgl.close()\n```\n\n::: {.cell-output .cell-output-error}\n\n```\nError in `rgl.close()`:\n! could not find function \"rgl.close\"\n```\n\n\n:::\n:::\n\n\n\n::: {.cell rgl.margin='0'}\n\n```{.r .cell-code}\nscene_luc <- readRDS(\"./_data/scene_luc_basin.rds\")\n```\n\n::: {.cell-output .cell-output-error}\n\n```\nError in `gzfile()`:\n! cannot open the connection\n```\n\n\n:::\n\n```{.r .cell-code}\nscene_luc %>%\n  plot_3d(\n    resize_matrix(srtm_array, 1 / 5),\n    zscale = 40,\n    linewidth = 0,\n    theta = 15,\n    phi = 45,\n    fov = 0,\n    zoom = 0.4,\n    family = \"Roboto Condensed\",\n    shadow = TRUE,\n    shadowcolor = pal[\"black\"],\n    solidcolor = pal[\"black\"]\n  )\n```\n\n::: {.cell-output .cell-output-error}\n\n```\nError in `plot_3d()`:\n! could not find function \"plot_3d\"\n```\n\n\n:::\n\n```{.r .cell-code}\nrglwidget()\n```\n\n::: {.cell-output .cell-output-error}\n\n```\nError in `rglwidget()`:\n! could not find function \"rglwidget\"\n```\n\n\n:::\n\n```{.r .cell-code}\nrgl.close()\n```\n\n::: {.cell-output .cell-output-error}\n\n```\nError in `rgl.close()`:\n! could not find function \"rgl.close\"\n```\n\n\n:::\n\n```{.r .cell-code}\n#save_3dprint(\"mli_basin.stl\", maxwidth=\"120mm\", clear=TRUE)\n#save_obj(\"mli_basin.obj\")\n```\n:::\n\n\n\n# 3D Processing with Three.js\n\nFor comparison's sake we build a similar scene using **Three.js** libraries instead of WebGL. The scene is **12 MB in size** (using a low resampling rate), it was generated using the [Qgis2threejs](https://qgis2threejs.readthedocs.io/en/docs/) plugin. The main difference is we're showing **ESA CCI land cover classification** as an image overlay instead of a texture.\n\nThe [widget is rendered](./threejs_files/) here. \n\nIn [Part 4](./2021-11-06-deckgl) we experiment with another WebGL-derived Javascript library [Deck.gl](https://deck.gl/) and 3D rendering of terrain layers.\n",
    "supporting": [],
    "filters": [
      "rmarkdown/pagebreak.lua"
    ],
    "includes": {},
    "engineDependencies": {},
    "preserve": {},
    "postProcess": true
  }
}